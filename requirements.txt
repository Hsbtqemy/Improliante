# Dépendances d'EXÉCUTION de l'application.
# Stack arrêtée dans docs/cahier-des-charges-asso.md §2.
#
# Versions ÉPINGLÉES à ce que la suite de tests éprouve réellement : deux
# installations du même commit doivent produire le même environnement. Avec des
# minimums (`Django>=6.0`), le serveur installait un jour 6.0 et le lendemain
# 6.1 — et ne faisait plus tourner le code qui a été testé.
#
# Mettre à jour : relever une version dans un venv neuf, lancer `pytest`, puis
# épingler ici ce qui passe. Limite assumée : les dépendances TRANSITIVES ne
# sont pas figées ; un verrou complet (pip-compile) a sa place avec la barrière
# d'intégration (OPS-01).
#
# Python 3.12 minimum, exigence de Django 6 ; éprouvé sur 3.14.
#
# Les outils de développement vivent dans requirements-dev.txt : le serveur n'a
# besoin ni de pytest ni de ruff.

# --- Socle ------------------------------------------------------------------
Django==6.0.6
djangorestframework==3.17.1      # installé, pas encore activé (aucune vue DRF)
psycopg[binary]==3.3.4           # pilote PostgreSQL (v3)
python-dotenv==1.2.2             # chargement .env en développement

# --- Sécurité ---------------------------------------------------------------
# Les quatre suivantes sont installées mais COMMENTÉES dans `INSTALLED_APPS` :
# leur activation relève du durcissement fait au déploiement (cf. DEP-1).
argon2-cffi==25.1.0              # hachage des mots de passe (Argon2, cf. §10) — actif
django-axes==8.3.1               # anti-brute-force
django-otp==1.7.0                # 2FA (admin/bureau)
qrcode==8.2                      # QR codes TOTP
django-csp==4.0                  # CSP — Django 6 en propose une native, à arbitrer
django-simple-history==3.12.0    # journal d'audit (qui a fait quoi)

# --- Permissions / arbres ---------------------------------------------------
django-guardian==3.3.2           # permissions par objet — non utilisé à ce jour
django-treebeard==5.3.0          # arborescence GED (Dossier auto-référent) — actif

# --- Images / médias --------------------------------------------------------
Pillow==12.3.0                   # traitement d'images (ImageField, vignettes)

# --- Documents (PDF / Excel) -----------------------------------------------
# WeasyPrint nécessite des dépendances SYSTÈME. Depuis la v60 ce ne sont NI cairo
# NI gdk-pixbuf, contrairement à ce qui se répète un peu partout : la v69 ouvre
# glib (gobject), pango, pangoft2, harfbuzz, harfbuzz-subset et fontconfig — la
# liste exacte se lit dans `weasyprint/text/ffi.py`, et la seule éprouvée sur une
# Debian est celle de `.github/workflows/verification.yml`, qui la rejoue.
# Voir https://doc.courtbouillon.org/weasyprint/stable/first_steps.html
WeasyPrint==69.0                 # génération PDF (devis, factures, reçus)
openpyxl==3.1.5                  # exports Excel (budget)

# --- Serveur de production --------------------------------------------------
gunicorn==26.0.0                 # serveur WSGI (derrière Nginx, cf. deploiement/)
